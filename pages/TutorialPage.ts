import { browser, element, by, protractor, $,$$} from 'protractor';

export class TutorialPage{

    //Open Browser
    VerifyTutoraialPageTitle(){
            return browser.getTitle();
         };
        
    }  
    
